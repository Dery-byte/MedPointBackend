mnotify:
  v2:
    url: https://api.mnotify.com/api/sms/quick
    key: oWmpfPGz22BObP6ng9Zjb0Xe4
    sender-id: OPTUMSERV
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/medpointdb?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: EMMARESCUE
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  mail:
    host: smtp.gmail.com
    port: 587
    username: emmanuelderryshare@gmail.com
    password: xkgafkycoljlxvew
    protocol: smtp
    properties:
      mail:
        debug: false
        smtp:
          auth: true
          starttls:
            enable: true       # ✅ corrected
            required: true     # ✅ optional but good to include
          connectiontimeout: 5000
          timeout: 3000
          writetimeout: 5000
          ssl:
            trust: smtp.gmail.com
logging:
  level:
    root: INFO
    com:
      medpoint: DEBUG
    org:
      springframework: INFO
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE
  file:
    name: logs/medpoint.log
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
paystack:
  secret-key: ${PAYSTACK_SECRET_KEY}
  public-key: ${PAYSTACK_PUBLIC_KEY}
  base-url: https://api.paystack.co
  callback-url: http://localhost:8080/api/payments/callback
  app:
    upload:
      dir: uploads
    front-end: http://localhost:5173
    base-url: http://localhost:8080/api
    dev-mode: false
  server:
    port: 8080
    servlet:
      context-path: /api
  jwt:
    secret: medpoint-super-secret-key-must-be-at-least-256-bits-long-change-in-prod
    expiration-ms: 86400000
  logging:
    level:
      com.medpoint: DEBUG
      org.springframework.security: WARN